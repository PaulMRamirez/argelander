/** Phase 1 deliverable (ADR-0003). Surface: strips in, a Leaflet layer out. */
export const VERSION = '0.0.1';
export interface LeafletAdapterInit {
  /** Placeholder; frozen in Phase 1 against SPEC-STRIP. */
  layerId: string;
}
